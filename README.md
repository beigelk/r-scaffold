# r-scaffold
Sets up an R project using a pre-defined framework.

Current options:
    - `basic`

## Usage
Use `-c` to specify the JSON config file.
```bash
bash src/setup.sh -c templates/base/config.json
```